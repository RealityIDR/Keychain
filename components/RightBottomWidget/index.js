import React from 'react';
import { View, Text } from 'react-native';

import styles from './styles';

const RightBottomWidget = (props) => {
    return (
        <View style={styles.container}></View>
    );
};

export default RightBottomWidget;